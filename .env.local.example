# ============================================================
# Letterin - 环境变量配置示例
# 复制此文件为 .env.local 并填入真实值
# ============================================================

# 数据库 (Neon PostgreSQL)
# 从 https://neon.tech 获取
DATABASE_URL=postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# 认证
# 使用以下命令生成密码 hash：
# node -e "const b=require('bcryptjs');b.hash('your-password',10).then(h=>console.log(h))"
ADMIN_PASSWORD_HASH=$2b$10$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 随机32位字符串，用于 session 加密
SESSION_SECRET=your-random-32-char-secret-here-change-this

# AI 功能 (用于内容穿透率评分)
# 从 https://console.anthropic.com 获取，可选
ANTHROPIC_API_KEY=sk-ant-api03-xxx

# 应用配置
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Newsletter 的 URL slug（你的 Newsletter 标识符）
CREATOR_SLUG=my-newsletter

# ============================================================
# 以下配置也可在应用的 Settings 页面中设置
# ============================================================

# SMTP 邮件配置（可选，也可以在 Settings 页面配置）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your@email.com
SMTP_PASS=your-app-password
SMTP_FROM_EMAIL=your@email.com
SMTP_FROM_NAME=Your Newsletter Name
